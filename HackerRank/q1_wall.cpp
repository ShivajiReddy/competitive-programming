#include <bits/stdc++.h>

int main(int argc, char const *argv[])
{
	vector<int> a;
	vector<int> b;
	int n, m;
	cin >> n >> m;
	for (int i = 0; i < n; ++i)
	{
		cin >> a[i];
	}
	for (int i = 0; i < m; ++i)
	{
		cin >> b[i]''
	}

	return 0;
}