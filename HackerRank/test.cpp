#include <bits/stdc++.h>

using namespace std;
 
long long fast_exp(long long n, long long k)
{
	if(k == 0) return 1;
	if(k == 1) return n;
	long long p = 
}

int main(int argc, char const *argv[])
{
	ll n, k;
	cin >> n >> k;
	cout << n << " " << k << endl;
	//cout << fast_exp(n, k);
}