#include <bits/stdc++.h>
using namespace std;

int minute_two_winit(vector<int> v, int k)
{
	
}

int main(int argc, char const *argv[])
{
	int n k; 
	cin >> n >> k;
	vector<int> v(n);
	for (int i = 0; i < n; ++i) { cin >> v[i]; }
	cout << minute_two_winit(v, k);

	return 0;
}
